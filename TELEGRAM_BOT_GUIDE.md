# 📘 Инструкция по подключению Telegram-бота «Банный Клуб»

## 🎯 Обзор

Telegram-бот «Банный Клуб» автоматизирует работу с участниками сообщества: онбординг, запись на мероприятия, модерацию чата и аналитику.

---

## 📋 Содержание

1. [Создание бота в Telegram](#1-создание-бота-в-telegram)
2. [Настройка базы данных](#2-настройка-базы-данных)
3. [Разработка бота](#3-разработка-бота)
4. [Подключение к чату](#4-подключение-к-чату)
5. [Команды бота](#5-команды-бота)
6. [Сценарии использования](#6-сценарии-использования)
7. [Админ-панель](#7-админ-панель)

---

## 1. Создание бота в Telegram

### Шаг 1: Создайте бота через BotFather

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота: `Банный Клуб`
4. Введите username бота: `banya_club_bot` (или любой свободный)
5. Получите **API токен** — он понадобится для интеграции

**Пример:**
```
Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

### Шаг 2: Настройте команды бота

Отправьте BotFather команду `/setcommands` и вставьте:

```
start - Начать работу с ботом
help - Помощь и FAQ
events - Ближайшие мероприятия
register - Записаться на мероприятие
profile - Мой профиль
baths - Каталог бань
masters - Каталог пармастеров
community - О сообществе
admin - Админ-панель (только для модераторов)
```

### Шаг 3: Настройте параметры приватности

```
/setprivacy - Disable (чтобы бот видел все сообщения в группе)
/setjoingroups - Enable (чтобы бот мог быть добавлен в группы)
```

---

## 2. Настройка базы данных

База данных уже создана в админ-панели. Структура:

### Таблицы:

- **members** — участники клуба
- **member_preferences** — предпочтения (женская/мужская/совместная, мягкий/горячий пар)
- **events** — мероприятия
- **event_registrations** — записи на мероприятия

### Подключение:

В вашем проекте уже есть `DATABASE_URL` в secrets. Используйте его для подключения бота к БД.

---

## 3. Разработка бота

### Выбор технологии

**Рекомендуется Python + aiogram:**

```bash
pip install aiogram psycopg2-binary
```

### Структура проекта бота:

```
telegram-bot/
├── bot.py              # Главный файл
├── handlers/
│   ├── start.py        # Команда /start
│   ├── events.py       # Работа с мероприятиями
│   ├── registration.py # Запись на мероприятия
│   ├── profile.py      # Профиль участника
│   └── admin.py        # Админ-команды
├── database/
│   └── queries.py      # SQL-запросы
├── keyboards/
│   └── inline.py       # Клавиатуры
└── config.py           # Конфигурация
```

### Пример кода (bot.py):

```python
import asyncio
import logging
from aiogram import Bot, Dispatcher
from aiogram.filters import Command
from aiogram.types import Message
import os
import psycopg2

# Настройка логирования
logging.basicConfig(level=logging.INFO)

# Токен бота
BOT_TOKEN = os.environ.get('BOT_TOKEN')
DATABASE_URL = os.environ.get('DATABASE_URL')

# Инициализация бота
bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()

# Подключение к БД
def get_db():
    return psycopg2.connect(DATABASE_URL)

# Команда /start
@dp.message(Command("start"))
async def cmd_start(message: Message):
    await message.answer(
        f"🧖 Привет, {message.from_user.first_name}!\n\n"
        "Добро пожаловать в Банный Клуб — сообщество любителей русской бани.\n\n"
        "Что я умею:\n"
        "🗓 /events — Ближайшие мероприятия\n"
        "✍️ /register — Записаться на событие\n"
        "👤 /profile — Мой профиль\n"
        "🏛 /baths — Каталог бань\n"
        "🔥 /masters — Пармастера\n"
        "❓ /help — Помощь"
    )

# Команда /events
@dp.message(Command("events"))
async def cmd_events(message: Message):
    conn = get_db()
    cur = conn.cursor()
    
    cur.execute("""
        SELECT title, date, time, location, capacity, 
               (SELECT COUNT(*) FROM event_registrations WHERE event_id = events.id) as registered
        FROM events 
        WHERE date >= CURRENT_DATE 
        ORDER BY date, time 
        LIMIT 5
    """)
    
    events = cur.fetchall()
    
    if not events:
        await message.answer("📅 Пока нет запланированных мероприятий")
        return
    
    text = "🗓 *Ближайшие мероприятия:*\n\n"
    
    for event in events:
        title, date, time, location, capacity, registered = event
        text += f"📍 *{title}*\n"
        text += f"📅 {date.strftime('%d.%m.%Y')} в {time.strftime('%H:%M')}\n"
        text += f"🏛 {location}\n"
        text += f"👥 Записано: {registered}/{capacity}\n\n"
    
    text += "Для записи используй команду /register"
    
    await message.answer(text, parse_mode="Markdown")
    
    cur.close()
    conn.close()

# Команда /help
@dp.message(Command("help"))
async def cmd_help(message: Message):
    await message.answer(
        "📚 *Помощь*\n\n"
        "*Основные команды:*\n"
        "/events — Посмотреть мероприятия\n"
        "/register — Записаться на событие\n"
        "/profile — Мой профиль и предпочтения\n"
        "/baths — Каталог бань Москвы\n"
        "/masters — Каталог пармастеров\n\n"
        "*Правила сообщества:*\n"
        "• Уважение к участникам\n"
        "• Никакого флирта и навязчивости\n"
        "• Запрещены политика и религия\n"
        "• Без мата и агрессии\n\n"
        "По вопросам пиши @admin",
        parse_mode="Markdown"
    )

# Запуск бота
async def main():
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
```

---

## 4. Подключение к чату

### Шаг 1: Добавьте бота в группу

1. Откройте группу Telegram
2. Нажмите на название группы → "Добавить участников"
3. Найдите вашего бота по username
4. Добавьте бота

### Шаг 2: Сделайте бота администратором

1. Откройте группу → "Редактировать"
2. "Администраторы" → "Добавить администратора"
3. Выберите бота
4. Выдайте права:
   - ✅ Удалять сообщения
   - ✅ Блокировать пользователей
   - ✅ Закреплять сообщения
   - ✅ Приглашать пользователей

### Шаг 3: Проверьте работу

Отправьте в чат: `/start`

Бот должен ответить приветствием.

---

## 5. Команды бота

### Для участников:

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и онбординг |
| `/events` | Список ближайших мероприятий |
| `/register` | Запись на мероприятие |
| `/profile` | Профиль участника |
| `/baths` | Каталог бань с фильтрами |
| `/masters` | Каталог пармастеров |
| `/community` | Информация о клубе |
| `/help` | Помощь и правила |

### Для администраторов:

| Команда | Описание |
|---------|----------|
| `/admin` | Открыть админ-панель |
| `/stats` | Статистика чата |
| `/announce` | Создать анонс |
| `/ban` | Забанить участника |
| `/mute` | Замутить участника |

---

## 6. Сценарии использования

### 🆕 Онбординг нового участника

**1. Участник входит в чат**
```
Бот (в ЛС):
🧖 Привет! Добро пожаловать в Банный Клуб!

Расскажи немного о себе:
• Как тебя зовут?
• Какую баню предпочитаешь? (женская/мужская/совместная)
• Любишь мягкий или горячий пар?

Это необязательно, но поможет рекомендовать подходящие события 😊
```

**2. Участник отвечает**
```
Участник: Анна, женская баня, мягкий пар

Бот:
✅ Отлично, Анна! Твой профиль сохранён.

Я буду присылать тебе анонсы женских бань с мягким паром.

Ближайшие события: /events
```

---

### 📅 Запись на мероприятие

**1. Участник смотрит события**
```
Участник: /events

Бот:
🗓 Ближайшие мероприятия:

📍 Женская баня с пармастером
📅 05.12.2025 в 18:00
🏛 Баня на Сретенке
👥 Записано: 8/12

[Записаться] [Подробнее]
```

**2. Участник записывается**
```
Участник: [нажимает "Записаться"]

Бот:
✍️ Хорошо! Уточни несколько деталей:

• Имя: 
• Телефон:
• Предпочтения (купель, чай, веники):
```

**3. Подтверждение**
```
Участник: Анна, +7 900 123-45-67, купель + травяной чай

Бот:
✅ Готово! Ты записана на мероприятие.

📍 Женская баня с пармастером
📅 05.12.2025 в 18:00
🏛 Баня на Сретенке, ул. Сретенка, 5

Напомню за 24 часа и за 2 часа до начала.

[Отменить запись]
```

---

### 🚨 Модерация нарушений

**Автоматическая модерация:**

```
Участник: [отправляет мат]

Бот (удаляет сообщение):
⚠️ @username, пожалуйста, соблюдай правила клуба. 
Предупреждение 1/3.

Следующее нарушение — мут на 24 часа.
```

**Ручная модерация:**

```
Модератор: /ban @username причина: токсичность

Бот:
✅ Участник @username заблокирован.
Причина: токсичность
```

---

### 📊 Работа с каталогами

**Каталог бань:**
```
Участник: /baths

Бот:
🏛 Каталог бань Москвы

Выбери район:
[Центр] [САО] [СВАО] [ВАО] [ЮВАО] [ЮАО] [ЮЗАО] [ЗАО] [СЗАО]

Или используй фильтры:
[💰 Цена] [👥 Вместимость] [🌊 Купель] [🔥 Пармастер]
```

```
Участник: [выбирает "Центр"]

Бот:
🏛 Бани в центре Москвы:

1️⃣ Сандуны
📍 ул. Неглинная, 14
💰 3500₽/час
👥 до 15 человек
⭐️ 4.8/5.0

[Подробнее] [Забронировать]

2️⃣ Баня на Сретенке
📍 ул. Сретенка, 5
💰 2500₽/час
👥 до 12 человек
⭐️ 4.6/5.0

[Подробнее] [Забронировать]
```

---

## 7. Админ-панель

Админ-панель доступна по адресу вашего проекта на poehali.dev.

### Разделы:

**📊 Дашборд**
- Всего участников
- Активных сегодня
- Мероприятий в месяц
- Средняя посещаемость

**📅 Мероприятия**
- Создание нового события
- Список участников
- Редактирование
- Отметки о посещении

**👥 Участники**
- Профили участников
- Фильтры по формату и предпочтениям
- История мероприятий
- Статус (новичок/активный)

**📈 Аналитика**
- Популярные форматы
- Температурные предпочтения
- Средний возраст
- График активности

---

## 🔐 Безопасность

### 1. Храните токен бота в секрете

Никогда не публикуйте токен в открытом доступе. Используйте переменные окружения:

```python
BOT_TOKEN = os.environ.get('BOT_TOKEN')
```

### 2. Защитите админ-команды

```python
ADMIN_IDS = [123456789, 987654321]  # Telegram ID администраторов

@dp.message(Command("admin"))
async def cmd_admin(message: Message):
    if message.from_user.id not in ADMIN_IDS:
        await message.answer("❌ У вас нет доступа к этой команде")
        return
    
    # Админ-функционал
```

### 3. Валидация данных

Всегда проверяйте входные данные от пользователей:

```python
import re

def validate_phone(phone: str) -> bool:
    return bool(re.match(r'^\+?[78][\d\s\-\(\)]{10,}$', phone))

def validate_name(name: str) -> bool:
    return len(name) >= 2 and len(name) <= 50
```

---

## 📚 Полезные ссылки

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/BotFather)
- [Админ-панель Банного Клуба](https://ваш-домен.poehali.dev)

---

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь, что токен правильный
3. Проверьте подключение к БД
4. Посмотрите примеры кода выше

**Сообщество:** [https://t.me/+QgiLIa1gFRY4Y2Iy](https://t.me/+QgiLIa1gFRY4Y2Iy)

---

## ✅ Чек-лист запуска

- [ ] Создан бот через BotFather
- [ ] Получен API токен
- [ ] Настроены команды бота
- [ ] Отключена приватность (для чтения сообщений в группе)
- [ ] Бот добавлен в группу
- [ ] Бот назначен администратором
- [ ] Написан код бота (Python/Node.js)
- [ ] Подключена база данных
- [ ] Протестированы основные команды
- [ ] Настроена модерация
- [ ] Админ-панель доступна
- [ ] Бот работает 24/7

---

**Готово! Ваш Telegram-бот для Банного Клуба настроен и готов к работе.** 🚀
